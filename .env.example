# Google faculty sign-in (required to contribute / build courses)
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Optional allowlist. Empty = any Google-signed-in user may contribute.
ALLOWED_EMAILS=

# Edge Config is also hardcoded in src/lib/config.ts
# EDGE_CONFIG=
