# Google OAuth only (any Google account can sign in)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Optional overrides (defaults are in src/lib/config.ts)
# AUTH_URL=https://geospatialskills.in
# NEXT_PUBLIC_SITE_URL=https://geospatialskills.in
